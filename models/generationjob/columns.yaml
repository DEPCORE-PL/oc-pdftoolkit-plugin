# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        searchable: true
    template:
        label: Template
        type: text
        select: name  # The column to display (you can change this based on your requirement)
        relation: template

    title:
        label: Title
        type: text

    downloadLink:
        label: Download Link
        type: partial
        # adjust this to your own plugin/theme namespace & partial path
        path: $/depcore/pdftoolkit/partials/columns/download_link.php
        sortable: false
        searchable: false

    status:
        label: Status
        type: text
        select: >
            CASE
              WHEN downloadLink IS NOT NULL THEN 'ready'
              ELSE 'processing'
            END
        sortable: false
        searchable: false
